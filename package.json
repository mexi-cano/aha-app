{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm --filter @workspace/client --filter @workspace/api-server run build",
    "test": "pnpm -r --if-present run test",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm --filter @workspace/aha-domain run typecheck:test && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "packageManager": "pnpm@11.19.0",
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "0.4.1"
  },
  "devDependencies": {
    "prettier": "3.9.6",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": "22.x"
  }
}
